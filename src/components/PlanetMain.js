import React from 'react';
import planets from '../data.json';

const PlanetMain = () => {
    console.log(planets);
    return (
        <main>
            <div></div>
        </main>
    );
};

export default PlanetMain;
